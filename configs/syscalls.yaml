name: syscalls
description: Convert operators to syscalls
passes:

  - name: Simple operator conversions
    downwards:

      - name: add
        match:
          self:
            name: operator
            key: name
            value.regexp: "[-+*/<>]|==|<=|>=|\\.\\.[<=]"
        action:
          replaceName:
            with: syscall

